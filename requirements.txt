# SSL DAPT + LoRA Training Requirements for Qwen3

# Core ML libraries
torch>=2.1.0
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.27.0

# LoRA and quantization
peft>=0.10.0
bitsandbytes>=0.43.0

# Flash Attention 2 (optional, for faster training)
# Install separately: pip install flash-attn --no-build-isolation
# flash-attn>=2.5.0

# Logging and monitoring
wandb>=0.16.0
tensorboard>=2.16.0

# Configuration
pyyaml>=6.0
omegaconf>=2.3.0

# Utilities
tqdm>=4.66.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.4.0

# Development (optional)
# pytest>=8.0.0
# black>=24.0.0
# ruff>=0.3.0
